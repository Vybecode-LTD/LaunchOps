# Test and lint tooling. Install with: python -m pip install -r requirements-dev.txt
-r requirements.txt
pytest>=9.0,<10.0
pytest-asyncio>=1.0,<2.0
pytest-cov>=7.0,<8.0
ruff>=0.16.7,<0.17  # minor releases can change the default rule set
pip-audit>=2.10,<3  # CI: python -m pip_audit -r requirements.txt
