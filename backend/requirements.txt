fastapi>=0.115.6,<1.0
uvicorn[standard]>=0.34.0,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
httpx>=0.28,<1.0
asyncpg>=0.30,<1.0
alembic>=1.20,<1.21  # database migrations (backend/migrations)
sqlalchemy[asyncio]>=2.0.54,<2.1  # Alembic's database layer; the app itself queries with asyncpg
python-dotenv>=1.0,<2.0
anthropic>=1.6,<2  # Claude API client; streams, retries and resumes paused web research
httpx2>=2.13,<3  # the SDK's HTTP layer; services/claude.py handles its transport errors
PyJWT>=2.8,<3.0
cryptography>=50,<51  # Fernet: SMTP passwords are encrypted at rest
bcrypt>=4.0,<5.0
